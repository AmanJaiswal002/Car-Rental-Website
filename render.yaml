services:
  - type: web
    name: car-rental-booking-platform
    env: static
    buildCommand: cd client && npm i && npm run build
    staticPublishPath: ./client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
